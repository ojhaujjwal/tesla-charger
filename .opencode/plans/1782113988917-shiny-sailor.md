# Fix Lint Errors (7 `no-async-await` violations)

## Errors

All 7 errors are from `effect-lint(no-async-await)` across 2 files:

1. **`src/data-adapter/influx-db-sungather.data-adapter.ts:73`** - `async authenticate()` method
2. **`src/setup-tesla-partner-application.ts:7-44`** - 6 errors from `(async () => { ... })()` IIFE with `await fetch`/`await response.json()`

## Plan

### 1. Fix `src/data-adapter/influx-db-sungather.data-adapter.ts:73`

Remove `async` from the `authenticate()` method. The method body doesn't await anything — it just calls `Effect.runSync`. The `IDataAdapter` interface doesn't include `authenticate`, so no contract change needed.

```diff
-   async authenticate() {
+   authenticate() {
```

### 2. Fix `src/setup-tesla-partner-application.ts`

Rewrite the CLI script to use Effect + `NodeRuntime.runMain`, following the pattern from `src/generate-refresh-token.ts` and `src/tesla-client/index.ts`:

- Import `NodeRuntime`, `NodeServices`, `NodeHttpClient` from `@effect/platform-node`
- Import `HttpClient`, `HttpBody` from `effect/unstable/http`
- Use `Effect.fn` for the program logic that yields `HttpClient.HttpClient`
- Use `yield* httpClient.post(url, { headers, body: HttpBody.raw(JSON.stringify(...)) })` for HTTP calls
- Use `yield* response.json` + `yield* Effect.log(responseBody)` for logging
- Use `yield* response.json` via `Schema.decodeUnknownEffect` to parse access token
- Use `Effect.runSync` for config reads (already done, keep as-is)
- Run with `NodeRuntime.runMain(program.pipe(Effect.provide(layer)))`

The script does two sequential HTTP calls:
1. POST to `https://auth.tesla.com/oauth2/v3/token` to get `access_token`
2. POST to `https://fleet-api.prd.na.vn.cloud.tesla.com/api/1/partner_accounts` with the token

## Verification

```bash
npm run lint
```

Expected: 0 errors.<｜end▁of▁thinking｜>

<｜｜DSML｜｜tool_calls>
<｜｜DSML｜｜invoke name="plan_exit">
<｜｜DSML｜｜parameter name="plan" string="true">[{"content": "Remove `async` from `authenticate()` method in `src/data-adapter/influx-db-sungather.data-adapter.ts:73`"}, {"content": "Rewrite `src/setup-tesla-partner-application.ts` to use Effect APIs instead of async/await with raw fetch"}, {"content": "Verify with `npm run lint` - expect 0 errors"}]